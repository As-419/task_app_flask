# Variables lues automatiquement par la commande `flask` (via python-dotenv).
# Ce fichier ne contient AUCUN secret : il peut être versionné dans Git.

FLASK_APP=run.py
FLASK_CONFIG=development
FLASK_DEBUG=1
